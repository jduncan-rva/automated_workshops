kind: ReplicationController
apiVersion: v1
metadata:
  name: app-rc
  labels:
    name: app-rc
spec:
  replicas: 1
  selector:
    name: app-rc
  template:
    metadata:
      labels:
        name: app-rc
    spec:
      containers:
      - name: app-rc
        image: docker.io/jeduncan/php-demo-app
        env:
        ports:
          - name: web-port
            containerPort: 8080
