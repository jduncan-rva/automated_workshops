apiVersion: v1
kind: Service
metadata:
  labels:
    name: app-eph-service
  name: app-eph-service
  namespace: default
spec:
  sessionAffinity: ClientIP
  type: NodePort
  ports:
  - name: web-registry
    protocol: TCP
    port: 8080
    targetPort: 8080
  selector:
    name: app-eph
  deprecatedPublicIPs:
  - {{ hostvars['kube1'].ip }}
